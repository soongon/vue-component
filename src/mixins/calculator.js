export default {
    methods: {
        addTwoNumbers(one, two) {
            return one + two;
        },
        subTwoNumbers(one, two) {
            return one - two;
        }
    },
    data() {
        return {

        }
    }
}